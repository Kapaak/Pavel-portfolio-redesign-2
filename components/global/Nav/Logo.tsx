//libs
import styled from "styled-components";

const Logo = () => {
	return <StyledLogo>PZ</StyledLogo>;
};

export default Logo;

const StyledLogo = styled.div`
	font-size: var(--fosi-nav);
	margin-left: var(--horizontal-gap);
`;
